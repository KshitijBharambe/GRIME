# ============================================================================
# Docker Compose - Base Configuration
# GRIME
#
# This file defines the base services that are shared across all environments.
# Override with docker-compose.dev.yml or docker-compose.prod-sim.yml
# ============================================================================

services:
  # ==========================================================================
  # PostgreSQL Database
  # ==========================================================================
  postgres:
    image: postgres:17-alpine
    container_name: grime-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-admin}
      POSTGRES_DB: ${POSTGRES_DB:-data_hygiene}
    env_file:
      - ../../.env
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ../../scripts/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
    networks:
      - backend-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-admin} -d ${POSTGRES_DB:-data_hygiene}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ==========================================================================
  # MinIO - S3-Compatible Object Storage (Local GCS Simulation)
  # ==========================================================================
  minio:
    image: minio/minio:latest
    container_name: grime-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    env_file:
      - ../../.env
    ports:
      - "127.0.0.1:${MINIO_API_PORT:-9000}:9000"
      - "127.0.0.1:${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio-data:/data
    networks:
      - backend-network

  # ==========================================================================
  # MinIO Client - Initialize Buckets
  # ==========================================================================
  minio-init:
    image: minio/mc:latest
    container_name: grime-minio-init
    depends_on:
      - minio
    env_file:
      - ../../.env
    entrypoint: >
      /bin/sh -c "
      attempt=0;
      until mc alias set local http://minio:9000 $$STORAGE_ACCESS_KEY $$STORAGE_SECRET_KEY || [ $$attempt -ge 30 ]; do
        attempt=$((attempt+1));
        echo 'Waiting for MinIO... attempt' $$attempt;
        sleep 2;
      done;
      if [ $$attempt -ge 30 ]; then echo 'MinIO did not become available'; exit 1; fi;
      mc mb local/${STORAGE_BUCKET:-grime-local} --ignore-existing;
      mc anonymous set download local/${STORAGE_BUCKET:-grime-local};
      echo 'MinIO buckets initialized successfully';
      "
    networks:
      - backend-network

  # ==========================================================================
  # Backend API (FastAPI)
  # ==========================================================================
  backend:
    build:
      context: ../..
      dockerfile: docker/backend/Dockerfile
      target: ${BUILD_TARGET:-development}
      args:
        BUILDKIT_INLINE_CACHE: 1
    container_name: grime-backend
    restart: unless-stopped
    depends_on:
      - postgres
      - minio
    env_file:
      - ../../.env
    environment:
      # Runtime constants (sourcing secret/config values from env_file)
      STORAGE_ENDPOINT: http://minio:9000
      API_HOST: 0.0.0.0
      API_PORT: 8000
      JWT_ALGORITHM: HS256
      JWT_EXPIRE_MINUTES: 30

      # Python
      PYTHONPATH: /app
      PYTHONDONTWRITEBYTECODE: 1
      PYTHONUNBUFFERED: 1
    ports:
      - "${API_PORT:-8000}:8000"
    networks:
      - frontend-network
      - backend-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ==========================================================================
  # Frontend (Next.js)
  # ==========================================================================
  frontend:
    build:
      context: ../..
      dockerfile: docker/frontend/Dockerfile
      target: ${BUILD_TARGET:-development}
      args:
        BUILDKIT_INLINE_CACHE: 1
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
    container_name: grime-frontend
    restart: unless-stopped
    depends_on:
      - backend
    env_file:
      - ../../.env
    environment:
      # API URLs
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
      INTERNAL_API_URL: http://backend:8000

      # Next.js
      NODE_ENV: ${NODE_ENV:-development}
      NEXT_TELEMETRY_DISABLED: 1

      # [TUNNEL-FIX] Explicitly set the tunnel host to ensure Next.js allows the connection.
      DEV_TUNNEL_HOST: "grime.kshitij.space"

      # Authentication (if using NextAuth)
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:3000}
      # NEXTAUTH_SECRET is loaded from ../../.env via env_file
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    networks:
      - frontend-network
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:3000', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

# ============================================================================
# Networks
# ============================================================================
networks:
  frontend-network:
    name: grime-frontend-network
    driver: bridge

  backend-network:
    name: grime-backend-network
    driver: bridge
    # In prod-sim, this should be internal: true

# ============================================================================
# Volumes
# ============================================================================
volumes:
  postgres-data:
    name: grime-postgres-data

  minio-data:
    name: grime-minio-data
