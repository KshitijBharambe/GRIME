/**
 * Re-exports from the canonical threshold definitions in lib/thresholds.ts.
 * Kept for backward compatibility with existing imports.
 */
export {
  getScoreColor,
  getScoreBgColor,
  getScoreBadgeVariant,
  getScoreLabel,
} from "@/lib/thresholds";
